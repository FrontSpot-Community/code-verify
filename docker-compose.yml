version: "3"
services:
  code-verify:
    build: .
    volumes:
      - ./app:/app/app
      - ./tools:/app/tools
    ports:
      - "3002:3000"
    links:
      - mongo
    depends_on:
      - mongo
  mongo:
    image: mongo
    volumes:
      - ./db:/data/db